WINDOW_WIDTH = 500
WINDOW_HEIGHT = 500


def px(pixels):
    return pixels*100 / WINDOW_WIDTH


def pc_w(pixels):
    return pixels*100/WINDOW_WIDTH


def pc_h(pixels):
    return pixels*100/WINDOW_HEIGHT
